{
  "name": "binaryyard",
  "version": "1.0.0",
  "description": "\"This is a project for learning tailwind css and gh-pages deployment\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist",
    "build:css": "postcss src/style.css -o src/style.css"
  },
  "author": "haibint",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "postcss": "^7.0.14",
    "tailwindcss": "^0.7.4"
  }
}
